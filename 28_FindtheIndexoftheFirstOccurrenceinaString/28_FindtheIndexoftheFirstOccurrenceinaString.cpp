class Solution {
public:
    int strStr(string haystack, string needle) {
        bool found = false;
        int len_word = needle.size();
        
        for (int i = 0; )

    }
};

'''
sliding window of needle over haystack finding keyword
using position + end of word to not go over
'''